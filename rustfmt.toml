# Formatting is enforced in CI (`cargo fmt --check`). Keep this minimal and
# stable-only so it works on the pinned toolchain without nightly features.
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
